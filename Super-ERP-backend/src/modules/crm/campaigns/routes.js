// Compatibility-preserving CRM Campaigns route boundary.
// Business logic remains in the existing controller until the controller migration task.
module.exports = require('../../../routes/campaignRoutes');
