// Compatibility-preserving CRM Offers route boundary.
// Business logic remains in the existing controller until the controller migration task.
module.exports = require('../../../routes/offerRoutes');
